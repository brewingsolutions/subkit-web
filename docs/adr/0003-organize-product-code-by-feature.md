# Organize product code by feature

Route-specific product code is organized under `features/` so its content, types, server implementation, client implementation, and tests retain locality. `components/ui`, `components/layout`, and genuinely shared visuals remain shared; generic constants and types folders do not become alternate domain owners.

